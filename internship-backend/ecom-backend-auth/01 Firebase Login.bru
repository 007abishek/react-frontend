meta {
  name: Firebase Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "firebaseIdToken": "{{firebaseIdToken}}"
  }
}
